kprobe:vfs_open
/comm == "file_opener"/
{
  $path = (struct path *)arg0;
  
  printf("Opened file: name=%s, inode=%d, mode=0%o\n",
         str($path->dentry->d_name.name),
         $path->dentry->d_inode->i_ino,
         $path->dentry->d_inode->i_mode);
}
